package com.example.pillbestie.ui.speech

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import com.example.pillbestie.data.MedicineRepository

class SpeechViewModel(application: Application, private val medicineRepository: MedicineRepository) : AndroidViewModel(application) {
    // Add functions for speech recognition, etc.
}
