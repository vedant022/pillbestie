package com.example.pillbestie.ui.medicine

import androidx.lifecycle.ViewModel
import com.example.pillbestie.data.MedicineRepository

class MedicineDetailViewModel(private val repository: MedicineRepository) : ViewModel() {
    // Add functions to get medicine details, etc.
}
